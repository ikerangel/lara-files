APP_NAME=lara-files
APP_ENV=local
APP_KEY=base64:cVrYLHTZ7nU5AM9nYl3uqIJ1QN5bLs+zQZDRI2m0AxY=
APP_DEBUG=true
APP_URL=http://lara-files.test
# Frontend is part of Laravel (Blade serves SPA)
FRONTEND_URL=${APP_URL}

# Session should use same root domain
SESSION_DOMAIN=lara-files.test
SESSION_LIFETIME=43200  # 30 days for long-lived login
SESSION_SECURE_COOKIE=false  # Only false if using HTTP in dev

# Sanctum config to trust frontend
SANCTUM_STATEFUL_DOMAINS=lara-files.test

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite


BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync


MEMCACHED_HOST=127.0.0.1

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_ENV=development
VITE_APP_NAME="${APP_NAME}"
VITE_APP_URL="${APP_URL}"
VITE_DEV_SERVER_URL=http://localhost:5173
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
